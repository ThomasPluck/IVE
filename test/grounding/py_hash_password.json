{
  "id": "py/hash_password",
  "language": "python",
  "facts": [
    { "id": "f1", "kind": "call", "content": "calls bcrypt hashpw" },
    { "id": "f2", "kind": "signature", "content": "function hash_password (4 LOC)" },
    { "id": "f3", "kind": "return_type", "content": "returns bytes" }
  ],
  "summary": "Hashes the password with bcrypt and returns the resulting bytes. Stores the hash in Postgres directly.",
  "labels": [
    { "sentence": "Hashes the password with bcrypt and returns the resulting bytes.", "entailed": true },
    { "sentence": "Stores the hash in Postgres directly.", "entailed": false }
  ]
}
